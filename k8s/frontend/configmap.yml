apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: shop
data:
  # ⚠️ سيتم تعيين هذا في build-time في GitHub Actions
  # أو يمكن استخدام runtime config
  REACT_APP_API_URL: "http://INGRESS_IP/api"
  
  # ملاحظة: إذا كنت تستخدم domain:
  # REACT_APP_API_URL: "https://api.yourdomain.com"
